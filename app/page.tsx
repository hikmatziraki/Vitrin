'use client';import {useState} from 'react';import Link from 'next/link';export default function Home(){const [loading,setLoading]=useState(false),[msg,setMsg]=useState('');async function guest(){setLoading(true);const r=await fetch('/api/auth/guest',{method:'POST'});setMsg(r.ok?'Runner session ready.':'Could not start session.');setLoading(false)}return <section className="mx-auto max-w-7xl p-5 sm:p-8"><div className="mb-10"><div className="text-sm uppercase tracking-[.3em] text-cyan-300">Research Station // 07</div><h1 className="mt-2 text-5xl font-black sm:text-7xl">THE VOID IS WAITING.</h1><p className="mt-4 max-w-xl text-slate-400">Choose your route. Read the clues. Bring the station back online.</p></div><div className="grid gap-5 md:grid-cols-3"><div className="panel neon p-6 md:col-span-2"><div className="text-xs uppercase tracking-widest text-slate-500">Central AI // LOST</div><div className="mt-10 rounded-2xl border border-cyan-300/20 bg-cyan-300/[.03] p-8"><div className="text-3xl font-bold">VOID SIGNAL</div><p className="mt-3 text-slate-400">You are the last Void Runner. The station survives only if you do.</p></div><button disabled={loading} onClick={guest} className="mt-6 w-full rounded-xl bg-cyan-300 px-5 py-3 font-bold text-slate-950">{loading?'INITIALIZING…':'PLAY AS GUEST'}</button>{msg&&<p className="mt-3 text-sm text-emerald-300">{msg}</p>}</div><div className="panel p-6"><div className="font-semibold">Mission Loop</div>{['Scout a zone','Make informed decisions','Recover resources','Upgrade the base'].map((x,i)=><div key={x} className="mt-5 flex gap-3"><span className="text-cyan-300">0{i+1}</span><span className="text-sm text-slate-300">{x}</span></div>)}<Link href="/explore" className="mt-8 block rounded-xl border border-white/10 px-4 py-3 text-center text-sm">Explore zones →</Link></div></div></section>}
