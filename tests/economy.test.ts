import {describe,it,expect} from 'vitest';import {moduleCost,moduleTime,successProbability,marketTax,emissionCap} from '../lib/economy';
describe('VOIDRUN economy',()=>{it('matches module curve',()=>{expect(moduleCost(100,1)).toBe(100);expect(moduleCost(100,2)).toBe(135);expect(moduleTime(2,1)).toBe(2)});it('clamps probability',()=>{expect(successProbability(1,1,0,0)).toBe(.95);expect(successProbability(0,0,1,0)).toBe(.15)});it('splits market tax',()=>{expect(marketTax(100)).toEqual({burn:2,treasury:2,rewards:1,total:5})});it('governs emission',()=>expect(emissionCap(1000,1,500)).toBe(500))});
